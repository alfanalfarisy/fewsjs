extends layout/base
block content
    .row
        .col-12
            .card#mapid(style="width:100%; height:100vh")

block script    
    script.
        var socket = io.connect(BASE_URL);
        socket.on('newestDps',function(msg){
            plot(msg.siteData,msg.newestDps)
        })
    script(src='/js/mapPlot.js')